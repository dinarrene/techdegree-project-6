extends layout.pug

block content
 
    h1= error.message
        h2= error.status
        p Oops! It looks like something went wrong with the server. 
        pre= error.stack

        a.home(href=`/`) Go Back!