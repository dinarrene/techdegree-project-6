extends layout.pug

block content

    h1= err.message
    h1= err.status
    p The page that you requested cannot be found. Please re-enter a valid path.
    pre= err.stack

    a.home(href=`/`) Go Back!
 

    