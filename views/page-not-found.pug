extends layout.pug

block content
    h1= error.message
    h1= error.status
    p The page that you requested cannot be found. Please re-enter a valid path.
    pre= error.stack

    a.home(href=`/`) Go Back!
 

    